# Product Requirements: Task Management API

## Overview
A REST API for managing tasks with user authentication.

## Features
1. User Authentication (register, login, JWT tokens)
2. Task CRUD (create, read, update, delete tasks)
3. Task filtering (by status, due date)
4. User profile management

## Technical Requirements
- Python with Flask or FastAPI
- SQLite for storage
- JWT for authentication
